import 'package:flutter/material.dart';

Future<void> initCamera() async {}
Widget buildCameraView() => const Center(child: CircularProgressIndicator());
Future<String?> capturarFrame(dynamic a, dynamic b) async => null;